{
  "name": "ahwa",
  "version": "0.1.0",
  "private": true,
  "description": "Anofelis Web — tables en ligne pour Horreur à Arkham JCE (Cloudflare Workers + Durable Objects)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit && wrangler deploy --dry-run",
    "types": "wrangler types",
    "build:data": "node scripts/build.mjs",
    "test": "node scripts/test_room.mjs",
    "captures": "python3 scripts/captures.py"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.128.0"
  },
  "dependencies": {
    "partyserver": "^0.5.10"
  }
}
